<%= form_for(@post) do |p| %>

  <%= p.label :content %>
  <%= p.text_field :content %>

  <%= p.label :user_id %>
  <%= p.number_field :user_id %>

  <%= p.submit yield(:button_text), class: "btn btn-primary" %>
<% end %>
